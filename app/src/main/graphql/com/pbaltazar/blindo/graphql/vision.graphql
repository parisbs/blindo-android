mutation ImageDescription(
  $input: ImageDescriptionInput!
) {
  imageDescription(input: $input) {
    description
    descriptionConfidence
    descriptionTags
    descriptionLanguage
    imageText
    left
  }
}
